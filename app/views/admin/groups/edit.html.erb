<h2> edit group </h2>

<%= simple_form_for [:admin, @group] do |f| %>

  <div class="group">
    <%= f.input :title %>
  </div>

  <div class="group">
    <%= f.input :description %>
  </div>



  <%= f.submit "Submit", data: { disable_with: "Submitting..." } %>

<% end %>
